<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        #weather-container {
            background-color: #fff;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 400px;
            width: 100%;
        }

        #city-input {
            padding: 8px;
            margin: 8px;
            width: 70%;
        }

        #get-weather-btn, #current-location-btn {
            padding: 10px;
            margin: 8px;
            cursor: pointer;
            background-color: #4caf50;
            color: #fff;
            border: none;
            border-radius: 4px;
            font-size: 16px;
        }

        #weather-info {
            margin-top: 20px;
        }
    </style>
</head>
<body>

    <div id="weather-container">
        <h2>Weather App</h2>
        <input type="text" id="city-input" placeholder="Enter city name">
        <button id="get-weather-btn">Get Weather</button>
        <button id="current-location-btn">Use Current Location</button>
        <div id="weather-info">
            <!-- Weather details will be displayed here -->
        </div>
    </div>

    <!-- JavaScript for fetching and displaying weather data would go here -->

</body>
</html>
